# ================================
# PostgreSQL (Docker container)
# ================================
POSTGRES_DB=
POSTGRES_USER=
POSTGRES_PASSWORD=
POSTGRES_PORT=

# JDBC / Flyway (for Spring services)
CONNECTION_STRING_POSTGRESQL=jdbc:postgresql://postgres:${POSTGRES_PORT}/${POSTGRES_DB}
POSTGRESQL_USERNAME=${POSTGRES_USER}
POSTGRESQL_PASSWORD=${POSTGRES_PASSWORD}

SPRING_FLYWAY_URL=jdbc:postgresql://postgres:${POSTGRES_PORT}/${POSTGRES_DB}
SPRING_FLYWAY_USER=${POSTGRES_USER}
SPRING_FLYWAY_PASSWORD=${POSTGRES_PASSWORD}
